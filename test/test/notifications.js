const { axios, axiosB, assert, testUserA, testUserB } = require('./test')
const faker = require('faker')

describe('notifications', function () {
  it('should notify a user if a request is fulfilled')

  it('should notify owner of requested item')

  it('should notify owner and holder of dropped off item')

  it('should notify owner and holder of picked up item')
})